GitHub Pages 上傳方式
====================

把這個資料夾內的所有檔案與資料夾，原樣上傳到 GitHub repository 的同一個目錄：

index.html
assets/
  logos/
  reference/

不要只上傳 index.html，否則案例 Logo、兩張 PNG 與兩份 PDF 會找不到。

如果 repository 已啟用 GitHub Pages，index.html 所在資料夾就會成為網頁入口。
所有圖片與 PDF 都使用相對路徑，不依賴外部網站。
